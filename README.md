# pet-care-GUI

This repository contains the Pet Care GUI React application. It was bootstrapped with Create React App.

Run `npm install` to install dependencies and `npm start` to run the development server.

For full Create React App documentation, see https://facebook.github.io/create-react-app/
